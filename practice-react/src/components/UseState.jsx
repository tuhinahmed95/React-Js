import { useState } from "react";

const UseState = () => {
    return (
        <div>
            <h1>Number :</h1>
            <button>UseState</button>
        </div>
    );
};

export default UseState;