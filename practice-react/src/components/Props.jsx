

const Props = (props) => {
    return (
        <div>
            <h5>{props.firstTitle}</h5>
            <h6>{props.btn}</h6>
            <h1>{props.title}</h1>
            <h1>{props.desText}</h1>
        </div>
    );
};

export default Props;